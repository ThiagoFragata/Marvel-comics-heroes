import styled from "styled-components";

export const Container = styled.header`
    background-color: var(--black);
    padding: 1rem;

    .content {
        max-width: 1024px;
        width: 100%;
        margin: 0 auto;

        color: var(--white);
        font-size: 2rem;
        font-weight: 700;
    }
`;
